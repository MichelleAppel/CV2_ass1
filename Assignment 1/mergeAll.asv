function [] = mergeAll()
%UNTITLED3 Summary of this function goes here
%   Detailed explanation goes here

path = './Data/data';  % Your folder
file = fullfile(path, '*.pcd');
d = dir(file);

file_names = []
for k = 1:numel(d)
  filename = fullfile(path,d(k).name);
  if ~contains(filename, 'normal')
     disp(filename) 
  end
end

end

